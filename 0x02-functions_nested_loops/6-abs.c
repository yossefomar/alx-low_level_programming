#include "main.h"
/**
 * _abs - get the absolute value of a number
 *
 * @n: integere number
 *
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}

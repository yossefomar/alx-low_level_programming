#include "main.h"
/**
 * main - Entry point
 *print call put char function
 * Return: Always 0 (Success)
 */
int main(void)
{
	int _islower(int c)
	{
		if (c >= 'a' && c <= 'z')
			return (1);
		else
			return (0);
	}
}

#include "main.h"
/**
 * mul - check if it's alpha or not
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: 1 -> alpha | 0 -> other wise
 */

int mul(int a, int b)
{
	return (a * b);
}

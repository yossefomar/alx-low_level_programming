#include "main.h"
/**
 * add - add two numbers
 *
 * @f: first number
 *
 * @s: second number
 *
 * Return: return the sum of the two numbers
 */
int add(int f, int s)
{
	int answer;

	answer = f + s;
	return (answer);
}

#include "main.h"
#include <string.h>
/**
 * _strlen - return len of str
 *
 * @s: pointer of first element of string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
